import React from "react";

function WorkoutPlanner() {
  return(
    <div>
      This will use the workout planner api to give users access to curated workouts based on parameters like time, equipment, and location.
    </div>
  );
}

export default WorkoutPlanner;